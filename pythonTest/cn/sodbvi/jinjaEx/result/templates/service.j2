/**
 * 服务层
 */
package {{package}}.service;

import com.hi.webapp.common.service.SuperService;
import org.springframework.stereotype.Service;
import {{package}}.entity.ProKeyword;

@Service
public class {{name | replace(name | first,name | first| upper,1)}}Service extends SuperService<{{name | replace(name | first,name | first| upper,1)}}> {

}